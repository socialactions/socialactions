<h1>New Feed / API</h1>

<%= error_messages_for :action_source %>

<% form_for(@action_source) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Index', action_sources_path %>
