= render :partial => 'search_form'

#search_results
  - if @search.q
    %h2 Results
  = page_entries_info @actions if @search.q
  = render :partial => 'action', :collection => @actions
  = will_paginate @actions

      


